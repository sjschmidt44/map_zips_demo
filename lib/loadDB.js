'use strict'

const fs = require('fs')
const pg = require('pg')
const conString = 'postgresql://localhost:5432'

fs.readFile(`${__dirname}/../data/test.json`, (err, data) => func)

const loadRecord = (record) => {

}

const createDB = () => {

}
